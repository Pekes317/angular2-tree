import { TreeItemDirective } from './item.directive';

describe('MyappDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeItemDirective();
    expect(directive).toBeTruthy();
  });
});
